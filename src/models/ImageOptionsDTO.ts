import { ImageSizesDTO } from './ImageSizesDTO';

export interface ImageOptionsDTO {
  thumb: ImageSizesDTO;
  small: ImageSizesDTO;
  medium: ImageSizesDTO;
  large: ImageSizesDTO;
  zoom: ImageSizesDTO;
  zoom2x: ImageSizesDTO;
}

