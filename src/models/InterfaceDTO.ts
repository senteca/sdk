
export interface InterfaceDTO {
  key: string;
  label?: string;
  baseUrls?: string[];
  canonicalUrl?: string;
  languageCode: string;
  defaultCurrencyCode: string;
  themeKey?: string;
  themeConfig?: object;
  themeConfigType?: object;
  secret?: string;
}

